<h1>Chat room</h1>

<div id="chats">
	<%= render @chats %>
</div>

<%= form_for @chat do |f| %>
  <%= f.text_field :content, :size => 70 %>
  <%= f.hidden_field :room_id, :value => @room.id %>
  <br>
  <%= f.submit "投稿する" %>
<% end %>
